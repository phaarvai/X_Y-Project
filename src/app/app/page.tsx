import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function AppHomePage() {
  return (
    <PlaceholderPage
      title="Your X!Y workspace"
      description="Authenticated home for discovery, engagement, and role-based workflows."
      primaryHref="/onboarding/roles"
      primaryLabel="Choose roles"
    />
  );
}
