import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function OrganizationVerificationPage() {
  return (
    <PlaceholderPage
      title="Organization verification"
      description="Verification status and document checks will live here once backend review is connected."
      primaryHref="/app"
      primaryLabel="Go to workspace"
    />
  );
}
